#include <gtest/gtest.h>

#include "core/MasterClock.h"

namespace sezo {
namespace core {

TEST(MasterClockTest, AdvanceAndGetPosition) {
  GTEST_SKIP() << "TODO: advance by N frames and verify position matches.";
}

TEST(MasterClockTest, ResetClearsPosition) {
  GTEST_SKIP() << "TODO: set/advance then reset to zero.";
}

TEST(MasterClockTest, SetPositionOverrides) {
  GTEST_SKIP() << "TODO: set position and verify GetPosition matches.";
}

}  // namespace core
}  // namespace sezo
